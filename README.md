attack.log --> original log of an attack log///
attack_drained.txt --> Result of an applying drain algorithm on attack.log . masking dynamic elements such as Ip address or port number to <*>///
attak_drained_data.txt --> attackseq.py 's input . make attack_drained.txt readable for attackseq.py(main_branch)///
drain_seq.py --> read raw data(attack.log) and applying drain algorithm and make it sequence
